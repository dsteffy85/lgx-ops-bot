# =============================================================================
# Kochiku CI — LGX-OPS-BOT Docker image build → ECR
# =============================================================================
# Builds Docker image and pushes to ECR for SKI deployment.
# Image: 186216918747.dkr.ecr.us-west-2.amazonaws.com/deployable/lgx-ops-bot
# =============================================================================

test_command: script/ci

targets:
  - type: docker-build

build_artifacts:
  lgx-ops-bot:
    ecr_container: Dockerfile
    distributions:
      - el7
