# =============================================================================
# Kochiku CI — LGX-OPS-BOT Docker image build → ECR
# =============================================================================
# Builds Docker image and pushes to ECR for SKI deployment.
# Image lands at: 186216918747.dkr.ecr.us-west-2.amazonaws.com/deployable/lgx-ops-bot:<sha>
# =============================================================================

build_artifacts:
  lgx-ops-bot:
    ecr_container: Dockerfile

distributions:
  - el7
